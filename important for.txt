I am creating form builder plugin where user have the option to setup the form from the backend through drag and drop system. I have created a admin menu page and add the two columns the left column is the area of form and the right is the feilds tray where the user get the feild and put the the left area. So I need guidness I will created the div elements in the tray. So the main question is how to make function tray to put the element to the left area. 